<div id="songtag-dialog" class="dialog-box">
	<%= form_for :songtag, :html =>{:id=>'songtag_add_form'}, :url=>songtags_path do |f| %>
    	<%= f.hidden_field :uid, :value=>user.id %>

		<div class="songtag-add-dialog">
			<div class="title-bar">
				<%= f.text_field :title, :value=>"" %>
			</div>
		</div>

      <!-- table에 아이템이 추가되는 부분은 js로 작-->
      <ul name="add-song-list-in-new-songtag">
        <%= f.fields_for "song[]", Song.new do |song_form| %>
        testsdfsdf
        <% end %>
      </ul>

	    <div class="song-input">
	      <input type="text" name="mint-name" placeholder="민트 제목">
	      <input type="text" name="mint-youtube-link" placeholder="유튜브 링크">
	      <a href="#" name="add-song-to-table" onclick="songtag_form_add_song()">추가</a>
	    </div>
	    
	    <div class="song-submit">
			<%= button_tag(type:'submit') do%>
	    		<b>확인</b>
	    	<% end %>
	    </div>
  <% end %>
</div>
