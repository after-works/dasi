<%= form_for :feedback, :html=>{:'data-type'=>'json'}, :url=>feedbacks_path do |f| %>
	<%= f.hidden_field :uid, :value=>current_user.fb_uid %>
	<%= f.text_field :text %>
	<%= f.button "Submit" %>
<% end %>
